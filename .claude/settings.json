{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "\"${CLAUDE_PROJECT_DIR:-.}/.claude/hooks/check-manifests.sh\" 2>/dev/null || true",
            "timeout": 15,
            "statusMessage": "Checking skill and plugin manifests..."
          }
        ]
      }
    ]
  }
}
